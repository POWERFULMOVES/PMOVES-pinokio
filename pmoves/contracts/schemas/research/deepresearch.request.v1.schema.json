{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "DeepResearchRequest",
  "type": "object",
  "required": ["prompt"],
  "properties": {
    "prompt": {
      "type": "string",
      "description": "Primary instruction or question for the deep research agent."
    },
    "context": {
      "type": "array",
      "description": "Additional free-form context strings provided by the caller.",
      "items": {
        "type": "string"
      }
    },
    "metadata": {
      "type": "object",
      "description": "Arbitrary metadata to forward to the runner.",
      "additionalProperties": true
    },
    "notebook": {
      "type": "object",
      "description": "Notebook payload that should accompany the research run.",
      "additionalProperties": true
    },
    "model": {
      "type": ["string", "null"],
      "description": "Optional model hint (e.g. openrouter identifier)."
    },
    "resources": {
      "type": "object",
      "description": "External resources the runner can enrich the context with.",
      "properties": {
        "cookbooks": {
          "type": "array",
          "description": "List of Qwen cookbook paths or raw URLs to merge into context.",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": true
}
